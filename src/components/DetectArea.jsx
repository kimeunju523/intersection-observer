const DetectArea = ({ detectAreaRef }) => {
  return (
    <div
      ref={detectAreaRef}
      className="bg-white text-white w-full py-2 flex justify-center items-center text-xs"
    >
      DetectArea
    </div>
  );
};

export default DetectArea;
